# 3. Three’s a Charm. Write a script of you choice.

# This script will

# 1. become root user
sudo -i

# 2. install ssh server
apt -y update
apt -y upgrade
apt-get install -y openssh-server

# 3. start ssh server
service ssh start

# 4. create new user
adduser jtian

# 5. Add user to sudoers
echo "jtian   ALL=(ALL:ALL) ALL" >> /etc/sudoers

# 6. finish message
echo "SSH server has now been installed and activated. Use user jtian to login from another computer."

# As long as host port 2222 is forwarded to guest port, SSH connection should now work
# run in mac terminal: ssh jtian@127.0.0.1 -p 2222

